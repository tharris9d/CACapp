# CAC App Client

Angular 19 frontend application for CAC Card Tester.

## Development

Run `npm install` to install dependencies.

Run `ng serve` for a dev server. Navigate to `http://localhost:4200/`.

## Build

Run `ng build` to build the project.

